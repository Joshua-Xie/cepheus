# Sample GIT Credentials file.
# Simply replace the `<...>` items to match yours and save that in your home directory with any name you like.
# This would only be used if your enterprise GIT credentials and email were different than your public github credentials

git config --global user.name <your git username>
git config --global user.email <your email>
git config --global http.proxy http://proxy.example.com:80 <your proxy url or make it blank>
git config --global https.proxy http://proxy.example.com:80 <your proxy url or make it blank>
